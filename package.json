{
  "name": "fakeformfiller",
  "version": "1.0.0",
  "description": "A sophisticated Chrome extension that intelligently detects and fills web forms with realistic dummy data using Faker.js. Perfect for developers, QA testers, and anyone who needs to quickly populate forms during testing or development.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:faker": "esbuild tools/faker-entry.js --bundle --format=iife --global-name=faker --outfile=libs/faker.bundle.js",
    "watch:faker": "esbuild tools/faker-entry.js --bundle --format=iife --global-name=faker --outfile=libs/faker.bundle.js --watch",
    "build": "npm run build:faker"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^9.9.0",
    "esbuild": "^0.25.9"
  }
}
